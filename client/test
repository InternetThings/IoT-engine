
  <h1>Create rules</h1>
  <form id="CreateRuleSetForm">
    <div>
      <p>Message:
        <input id="message" type="text">
      </p>
      <select id="sensorList">
        {{> list_of_sensors}}
      </select>
      <select id="operatorValue">
        <option disabled selected>Choose an operator</option>
        <option value=">">&#62;</option>
        <option value="<">&#60;</option>
        <option value=">=">&#62;=</option>
        <option value="<=">&#60;=</option>
        <option value="=">=</option>
        <option value="!=">!=</option>
      </select>
      Target Value:
      <input id="targetValue" type="text">
    </div>
  </form>
  <p>
    <button id="AddConditionbtn">Add</button>
  </p>
  <p>
    <button id="SaveRuleSetbtn">Save</button>
  </p>

<template name="list_of_sensors">
  {{#each accessToken in get_accessTokens}}
  <option value="{{accessToken_id}}">{{accessToken._id}}</option>
  <!--<option value="{{accessToken.sensor}}">{{accessToken.type}}</option>-->
  {{/each}}
</template>
